export const metadata = {
  title: 'Raven Transition',
  description: 'Create a raven-style masked transition between two images',
};

import './globals.css';

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}

